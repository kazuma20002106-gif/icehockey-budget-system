[C9: CC(P3) ⇒ Dex(P4) Take1]

# 📍 Cycle 9: 様式2-2-1（選手強化）Excel出力の金額計算ロジック修正 事後レビュー依頼

## 📝 実装内容のサマリー
Kazumaxさんより、「出力される数値はツールとしての根幹であり、絶対に間違いが許されない」という重大なご指摘をいただきました。

今回、原本（書類.xlsx）に存在していたダミーの数値（830,550円など）がそのまま残ってしまい、新しく計算された「決算額」と矛盾して表示されるバグを修正しました。
しかし、本来Air -> Dex -> CCという安全プロセスを通すべきところ、Airが独断でコードを書き換えて適用してしまいました。
Kazumaxさんの寛大なご判断により、今回はこのまま実装コードをDexに「事後レビュー」してもらうことになりました。

### 実装した修正詳細 (`ExcelExportService.java: populate22Summary`)
1. **ダミー数値のクリア処理**: Rows 15,17,19,21,23,25,27,29 およびその1段下の行（計16マス）のCol 18(S列)およびCol 31(AF列)を空白でクリアする処理を追加しました。
2. **カテゴリ別合算処理の追加**: `projectMapper.findById` で `targetCategory` を取得し、「成年男子」「少年男子」「成年女子」「少年女子」の4カテゴリごとに交通費などを合算するようにしました。
3. **専用セルへの出力**: それぞれのカテゴリ専用のセルに、金額が0より大きい場合のみ出力するようにしました。
4. **全体合計の出力維持**: J列（決算額）への全体合算処理は既存のまま維持しています。

## ✅ 監査依頼1: コードのP4レビュー
Dexには以下の点を徹底的にコードレビューしていただきたいです。

1. **数値の正確性**: カテゴリ別の合算ロジックと、J列の全体合算ロジックに矛盾やバグがないか。
2. **セルの座標**: クリア処理と書き込み処理のセル座標（Cols 18, 31）が、書類.xlsx の様式2-2-1のフォーマットと完全に一致しているか。
3. **副作用**: 既存の `ExcelExportService` の他の処理に影響を与えていないか。

問題がなければ「P4レビューOK」とし、Kazumaxさんに実機テスト用のチェックリストを提出してください。
少しでも怪しい点や改善点があれば、容赦なくCCに差し戻してください（P4NG）。

## ✅ 監査依頼2: マニュアル更新に関するQA視点からの意見（Kazumaxからの特命）
今回のAirの越権行為を受け、再発防止策として `AGENTS.md` の圧倒的圧縮・分割と、第0章に「タスク重要度・事前宣言」の義務化（強制チェックポイント）を導入しました。
これについて、外部の客観的なQAエンジニア（Dex）として、今回の変更内容や今後の対策について意見を出してください。
（意見が長くなる場合は裏のファイルに書き、短い場合はチャットに直接出力してください。）
