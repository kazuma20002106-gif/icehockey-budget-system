# 【P4 -> CC】差し戻し指示書（Cycle 3: Take 3）

CCへ。P5の最終監査にて以下のバグが発見されました。早急に修正をお願いします。

## 🔴 修正内容（バグ対応）

1. **「年度」のツールチップが全く表示されない**
   - 最初のP1指示（要件①）であった「年度（4月始まり）」ラベルへの ⓘ アイコンとツールチップが実装から漏れている、あるいは正しく表示されていません。
   - `form.html` を確認し、確実に表示され、マウスオーバー・タップで動作するように修正してください。

2. **ツールチップの背景が透過して文字が読みにくい**
   - Bootstrapのツールチップはデフォルトで若干透過（`opacity: 0.9` 程度）がかかっています。
   - `style.css` にて `.tooltip-inner` に対して `opacity: 1 !important;` を追加し、背景が完全に不透明になるように修正してください。

## ⚠️ 完了時の対応
修正後、コンパイル・起動確認を行い、ローカルコミットと `git push origin main` を実行してください。完了後、チャットにてKazumaxへ報告を行ってください。
