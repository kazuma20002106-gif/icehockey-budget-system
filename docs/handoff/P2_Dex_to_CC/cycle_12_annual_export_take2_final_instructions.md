[C12: Dex(P2) => CC(P3)]

# Cycle 12 年度末決算ファイル一括出力 Take2 再監査 / CC向け最終指示書

## 判定

**条件付きOK。CCは Cycle 12A のみ実装してください。**

Air Take2は、前回Dex監査で止めた主要リスクをおおむね反映しています。
ただし、金額・Excel帳票の危険度が高いため、CCへ渡す実装範囲は以下に限定します。

- 今回やる: **Cycle 12A**
- 今回やらない: **Cycle 12B / 12C**

つまり、今回は年度末ブック生成の土台と、様式2-1 / 2-2 / 2-2-1の安全な出力までです。
`budget_allocations` テーブル、予算管理画面、様式2-3、大容量プレビューUIは次サイクル以降に分けてください。

---

## CCが最初に読むファイル

- `docs/handoff/P1_Air_Blueprint/cycle_12_annual_export_take2.md`
- `docs/handoff/P2_Dex_to_CC/cycle_12_original_formula_audit.md`
- `docs/handoff/P2_Dex_to_CC/cycle_12_annual_export_pre_audit.md`
- このファイル
- `docs/PROJECT_RULES.md`

このファイルが、CC向けの最終指示です。
Air Take2と矛盾する場合は、このファイルを優先してください。

---

## 実装対象: Cycle 12A

### 1. テンプレートを本物原本へ置き換える

本物原本:

`C:\Users\kazum\OneDrive\デスクトップ\フォルダー\アイスホッケー書類関連\②【報告様式】R7-宮崎国スポ強化戦略プロジェクト補助金 (1).xlsx`

を `src/main/resources/書類.xlsx` として使う方針で進めてください。

注意:

- `src/main/resources/` 配下にバックアップxlsxや検証用xlsxを増やさない。
- 置換後、テンプレートに下記の数式修正・外部リンク除去を必ず入れる。
- 置換前後のシート名一覧をP3報告書に記録する。

### 2. 外部リンクを完全に消す

本物原本には、以下の外部リンクが残っています。

`\\192.168.145.12\d\Users\003\Desktop\・③【報告様式】R6 トップチーム活用事業.xlsx`

Dex確認では、少なくとも以下の2セルに外部参照風の式があります。

| シート | セル | 元の式 |
|---|---:|---|
| `様式２－４　事業実施・実績報告書（トップチーム）` | `C4` | `'[1]報告書２－１'!C14` |
| `様式２－４　事業実施・実績報告書（トップチーム）` | `G7` | `'[1]報告書２－１'!G8:I8` |

CCはテンプレート置換後、外部リンクを消してください。

必須検証:

- `src/main/resources/書類.xlsx` 内に `externalLinks` が残っていないこと。
- 数式文字列に `[` が残っていないこと。
- 数式文字列に `\\192.168.145.12` が残っていないこと。
- 出力したxlsxにも同じ確認を行うこと。

外部参照セルを内部参照に直す場合、推測で参照先を作らないでください。
内部参照先が確定できない場合は、既存のキャッシュ値 `0` または空欄に固定し、P3報告書に記録してください。

### 3. 様式2-2の直値セルを数式化する

テンプレート側で、以下のセルを修正してください。

| セル | 修正式 |
|---|---|
| `AE17` | `'様式２－２－１　事業別決算書（ふるさと）'!J16` |
| `AE19` | `'様式２－２－１　事業別決算書（ふるさと）'!J18` |
| `AE21` | `'様式２－２－１　事業別決算書（ふるさと）'!J20` |
| `J27` | `SUM(Q27:AK27)` |

Air Take2では `J27 = SUM(Q27:AK28)` も候補でしたが、原本確認では28行側に対象値がないため、Dex指示では `SUM(Q27:AK27)` に固定します。

### 4. 2-2-1選手強化費の支出合計も修正する

原本の `様式２－２－１　事業別決算書（選手強化費）!J34` は、支出合計なのに `J26` と `J28` を含んでいません。
このままだと2-2-1単体の支出合計がズレる危険があります。

テンプレート側で以下へ修正してください。

`SUM(J16,J18,J20,J22,J24,J26,J28,J30,J32)`

または、対象経費合計と対象外経費の関係を維持するなら以下でも可です。

`J14+J32`

CCはどちらを採用したかP3に明記してください。

### 5. 出力する主要シート

Cycle 12Aの年度末一括出力では、少なくとも以下を1ブックに含めてください。

- `様式２－１`
- `様式２－２`
- `様式２－２－１　事業別決算書（選手強化費）`
- `様式２－２－１　事業別決算書（トップチーム活用)`
- `様式２－２－１　事業別決算書（ふるさと）`
- 既存の2-4 / 2-5 / 2-6出力

様式2-3はCycle 12Bへ送ります。今回実装しないでください。

### 6. 様式2-1に書くセル

原本基準で以下へ出力してください。

| 用途 | セル |
|---|---:|
| 提出日 年 | `AC3` |
| 提出日 月 | `AG3` |
| 提出日 日 | `AK3` |
| 団体名 前半 | `AB9` |
| 団体名 後半 | `AI9` |
| 代表者職・氏名 | `AB11` |
| 対象年度 | `E15` |
| 担当者 | `AA42` |
| TEL | `AA43` |
| FAX | `AA44` |
| E-mail | `AA45` |

Air Take2では `AF3/AJ3/AN3` が候補でしたが、原本ではそこは「年」「月」「日」のラベルです。
数値を書き込むセルは `AC3/AG3/AK3` です。

### 7. 2-2-1へ書く金額セル

#### 選手強化費 `budgetTypeId=1`

| 費目 | セル |
|---|---:|
| 交通費 | `J16` |
| 宿泊費 | `J18` |
| 旅行雑費 | `J20` |
| 駐車料金 | `J22` |
| 借用料 | `J24` |
| 報償費 | `J26` |
| 需用費 | `J28` |
| 役務費 | `J30` |
| 対象外経費 | `J32` |

#### トップチーム活用 `budgetTypeId=2`

| 費目 | セル |
|---|---:|
| 交通費 | `J16` |
| 宿泊費 | `J18` |
| 使用料賃借料 | `J22` |
| 報償費 | `J24` |
| 需用費 | `J28` |
| 役務費 | `J30` |
| 対象外経費 | `J32` |

トップチームは、Kazumax判断により今回は出力対象に含めます。
ただし原本の2-2側はトップチームの旅行雑費・駐車料・食糧費を参照していません。
トップチームに旅行雑費・駐車料など未対応費目の入力がある場合は、黙って捨てず、例外または明確な警告で止めてください。

#### ふるさと `budgetTypeId=3`

| 費目 | セル |
|---|---:|
| 交通費 | `J16` |
| 宿泊費 | `J18` |
| 旅行雑費 | `J20` |
| 需用費 | `J28` |
| 役務費 | `J30` |
| 対象外経費 | `J32` |

ふるさとは、原本の2-2側で旅費3項目が直値だったため、上記「様式2-2の直値セルを数式化する」の修正が必須です。
ふるさとに借用料・駐車料・報償費など未対応費目の入力がある場合も、黙って捨てず、例外または明確な警告で止めてください。

### 8. 集計ルール

集計は `long` で行い、`(int)` キャストは禁止です。

基本ルール:

- 交通費: 参加者ごとの `Expense.transportCost` 合計
- 宿泊費: 参加者ごとの `Expense.accommodationCost` 合計
- 旅行雑費: `ProjectSummaryExpense.travelMiscCost * 参加者数 * ProjectSummaryExpense.travelMiscDays`
- 駐車料金: `ProjectSummaryExpense.parkingCost`
- 借用料: `ProjectSummaryExpense.rentalCost`
- 報償費: `ProjectSummaryExpense.compensationCost`
- 需用費: `ProjectSummaryExpense.suppliesCost`
- 役務費: `ProjectSummaryExpense.serviceCost`

0円は原則として `0` または空欄のどちらかに統一してください。
既存実装の見た目に合わせる場合でも、計算上は必ず0として扱ってください。

### 9. 実装ファイル

想定される変更対象:

- `src/main/resources/書類.xlsx`
- `src/main/java/com/miyazaki/icehockey/budgetsystem/service/ExcelExportService.java`
- `src/main/java/com/miyazaki/icehockey/budgetsystem/controller/ActivityController.java`
- `src/main/java/com/miyazaki/icehockey/budgetsystem/controller/ExportController.java`
- 必要なら `src/main/resources/templates/export/*.html`
- `src/main/resources/application.properties`

今回変更禁止:

- `src/main/resources/schema.sql`
- `src/main/resources/mapper/*.xml`
- `budget_allocations` 関連のModel/Mapper/Controller/HTML
- 様式2-3の実装
- 大容量プレビューのタブ化

コード変更をするため、`app.version` は `v2.4.0` へ更新してください。

### 10. POI実装上の必須事項

- 数式セルには `setCellFormula(...)` を使う。
- 出力前に `workbook.setForceFormulaRecalculation(true)` を設定する。
- 可能なら `FormulaEvaluator.evaluateAll()` 相当で主要式の評価を試す。
- テンプレートのダミー値は出力前にクリアまたは上書きする。
- 不要なテンプレートシートを削除する場合、必要な集計シートまで消さないこと。
- シート名は31文字制限と重複に注意する。

---

## 検証条件

CCは最低限、以下を確認してP3報告書に書いてください。

### コマンド

1. `.\mvnw.cmd -q -DskipTests compile`
2. `target/classes/application.properties` の `app.version=v2.4.0` 確認

### Excel検証

出力xlsxを作成し、以下を確認してください。

- 外部リンクがない。
- 数式に `[` または `\\192.168.145.12` がない。
- 様式2-2の `AE17/AE19/AE21/J27` が数式になっている。
- 選手強化費2-2-1の `J34` が `J26` と `J28` を含む正しい支出合計になっている。
- 様式2-2の全体合計が、3つの2-2-1の合計と一致する。
- 選手強化費のみ、ふるさとのみ、トップチームを含むケースで合計がズレない。
- 交通費、宿泊費、旅行雑費、駐車料、借用料、報償費、需用費、役務費が0円/1円以上の両方で壊れない。
- 参加者0人の事業で落ちない。

### P3報告書

保存先:

`docs/handoff/P3_CC_to_Dex/cycle_12_annual_export_12a.md`

P3には以下を必ず含めてください。

- 変更ファイル一覧
- テンプレート置換前後のシート名一覧
- 外部リンク除去の確認結果
- 修正した数式セル一覧
- 集計ロジックの要約
- 検証コマンド結果
- 出力xlsxで確認した主要セルの値/数式
- 残した制約、未対応範囲

---

## Dexメモ

Air Take2はCCへ渡せる状態まで改善されています。
ただし、Air Take2に残っていた以下の曖昧さは、Dex指示として上書きしました。

- 様式2-1の日付セルは `AF3/AJ3/AN3` ではなく `AC3/AG3/AK3`。
- `J27` は `SUM(Q27:AK27)` に固定。
- 選手強化費2-2-1の `J34` 修正を必須化。
- Cycle 12B/12Cは今回対象外。
- 未対応費目は黙って捨てない。

CCはこの範囲で実装し、P3をDexへ戻してください。
