# Dex Cycle 8.3 追加提案

今回のようなExcel出力・金額計算・UI・検索が混在する危険タスクでは、1サイクルで全部を実装するとレビューが重くなりがちです。

次回以降は、可能なら以下のように分割することを提案します。

1. Excel出力・金額計算だけのサイクル
2. UI/UX改善だけのサイクル
3. 一覧検索だけのサイクル

ただし今回はロールバック済みバックログの再実装なので、Dex補強版P2でガードを厚くしたうえでCCに渡す運用で進めてよいと判断します。

